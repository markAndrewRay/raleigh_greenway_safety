# raleigh_greenway_safety

## Getting Started

To run this project locally, follow these steps:

1. **Clone the repository.**
2. **Download the data:** Run the setup script to fetch the large datasets (ignored by Git due to size):
   ```bash
   ./setup_data.sh